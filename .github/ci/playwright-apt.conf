// Scope only the ephemeral CI dependency installation to the image's signed
// Ubuntu sources. An unrelated Chrome feed must not prevent apt index refresh.
Dir::Etc::sourcelist "/etc/apt/sources.list.d/ubuntu.sources";
Dir::Etc::sourceparts "-";
