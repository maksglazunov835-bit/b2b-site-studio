services:
  b2b-site-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: b2b-site-studio
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
